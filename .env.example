# Rename to `.env.local` and fill values. Do NOT commit `.env.local`.

# --- Supabase ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only secret. Never expose to client code.
SUPABASE_SERVICE_ROLE_KEY=

# --- Admin / Email ---
ADMIN_EMAIL=
RESEND_API_KEY=

# SMTP (e.g., Mailtrap sandbox)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# --- App ---
NEXT_PUBLIC_APP_BASE_URL=http://localhost:3000

# --- Stripe ---
# Public key (safe to expose on client)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
# Server-only secret
STRIPE_SECRET_KEY=sk_test_...
# Set after creating webhook endpoint
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_CURRENCY=vnd
NEXT_PUBLIC_ENABLE_STRIPE=0

# --- VietQR (optional fallback) ---
NEXT_PUBLIC_VIETQR_BANK=
NEXT_PUBLIC_VIETQR_ACCOUNT=
NEXT_PUBLIC_VIETQR_ACCOUNT_NAME=

# --- VNPAY ---
VNP_TMN_CODE=
VNP_HASH_SECRET=
VNP_URL=https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
VNP_RETURN_PATH=/api/payments/vnpay/return
VNP_IPN_PATH=/api/payments/vnpay/ipn
VNP_LOCALE=vn
VNP_CURR_CODE=VND

# --- MoMo ---
MOMO_PARTNER_CODE=
MOMO_ACCESS_KEY=
MOMO_SECRET_KEY=
MOMO_ENDPOINT=https://test-payment.momo.vn/v2/gateway/api/create
MOMO_RETURN_PATH=/api/payments/momo/return
MOMO_IPN_PATH=/api/payments/momo/ipn
MOMO_REQUEST_TYPE=captureWallet
MOMO_LANG=vi

